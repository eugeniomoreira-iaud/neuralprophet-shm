% ---------------------------------------------------------------------------
% Study report · Grey-box expectation and monitoring of the station 02 inclination
%
% Build: pdflatex greybox_monitoring_report.tex (twice, for the ToC)
% Figures and table bodies are read from ../outputs/ and are produced by the
% paired notebook, which must be run first. Sections are written phase by
% phase; a section still to be written carries a \pending{} note.
% ---------------------------------------------------------------------------
\documentclass[11pt,a4paper]{article}

\newcommand{\runninghead}{Grey-box expectation and monitoring at Gubbio}

\input{../../_shared/reportstyle.tex}
\usepackage{longtable}
\graphicspath{{../outputs/}}

\newcommand{\pending}[1]{\begin{center}\textcolor{rulegrey}{\itshape
This section is written after #1. Nothing here is a result yet.}\end{center}}

\title{\vspace{-1.2cm}\textbf{Is this reading the one the wall was expected to show?}\\[2mm]
\large A grey-box NeuralProphet decomposition, rolling expectation and
three-scale monitor of the station 02 inclination, 2018 to 2026}
\author{Study report · \texttt{studies/05\_greybox\_monitoring/}}
\date{}

\begin{document}
\maketitle
\thispagestyle{fancy}

\tableofcontents

% ===========================================================================
\section{Introduction}
\label{sec:intro}

A reading arrives from the wall every twenty minutes: the inclination of the
instrument at station~02, the air temperature and relative humidity beside it,
and, since 21~February 2025, the temperature of the stone and the solar
radiation falling on it. The question a person watching that stream must be
able to answer is operational rather than diagnostic. Given the environment
measured at this instant, is this inclination the one the wall was expected to
show, and if it is not, which kind of departure is it? Study~04 posed that
question on the window after the long outage of 2022 to 2023 and answered part
of it. It also recorded its own weak points: an annual term that was not
identifiable on 3.2 interrupted cycles, and a trend that changed sign across
the thirds of its window; a conformal interval calibrated
once, eleven months before the record it was then asked to cover, that reached
67.7\,\% coverage against a nominal 90\,\%; a residual whose lag-one
autocorrelation of 0.995 pushed the control limit to 14.75 standard deviations
and left the detector blind to every slow mechanism; and a phase-change
injection scaled by the leftover daily seasonal term rather than by the wall's
own daily response, which overstated that blind spot by roughly five times.
This study keeps what Study~04 established and changes what it got wrong.

Four studies precede it, and their conclusions are inherited rather than
re-litigated. Study~01 read the raw archive, decided which recorded values are
measurements, and exported the inclination product used here and no other:
compensated for temperature with the manufacturer's linear correction,
anchored once across the whole record, cleaned of impulsive noise, and
accompanied by a flag that marks every value the cleaning replaced. It also
established that the two instruments installed at station~02 are continuous
across the changeover of 21~February 2025, and that the excursions of summer
2026 are an instrument or power artefact rather than a structural or
environmental event. Study~02 characterised the two external sources, the
town's ground station and the ERA5 reanalysis, against the on-structure
channels: the station is the closer proxy, ERA5 the more transferable one, and
both report global horizontal radiation, the quantity the wall's own
pyranometer measures. Study~03 measured the couplings: air temperature drives
the inclination instantaneously, with a diurnal-band gain near
$-2.8$\,mdeg/$^{\circ}$C on the on-structure channel; radiation acts with a
delay of about one hour; humidity follows the temperature cycle inversely
rather than acting as a mechanism of its own; and the wall temperature is an
internal state that leads the deformation and must not be used predictively at
its measured lead. Study~04 settled the modelling choices this study keeps: no
autoregression in the decomposition, because it absorbs the diurnal structure
the regressors should explain; changepoints placed on covered time; the level
decomposed and monitored rather than scored as a forecast; contemporaneous
regressors in the expectation treated as legitimate inputs rather than as
leakage; and no value of the target ever imputed, with the library's own gap
filling switched off.

Two boundaries hold throughout. The thermal compensation is taken as given: it
is the manufacturer's procedure, it has been validated against the user's
numerical model of the wall, and every gain this study learns is therefore a
post-compensation wall response and is labelled as such. No fit is made on the
raw channel. Forecasting is out of scope, because Study~04 answered it: the
skill of a forecast comes from the response's own memory, and the environment
adds nothing distinguishable from zero beyond six hours.

Within those boundaries the study asks five questions. What is the record made
of, over eight years: trend, annual cycle, daily cycle and the response to each
measured driver, with the share of variance each carries and the gain each
learns, on three regressor sets? Does the wall answer its drivers with a delay
or an inertia that the twenty-minute grid can resolve, when the response is
learned rather than imposed? Is this reading the expected one, given a rolling
expectation and an interval whose coverage is measured as a function of how
stale the last refit is? Which departure does each of three control charts
catch, at a false-alarm budget stated in advance, when the injected departures
are sized by the wall's measured daily response? And what happened across each
outage, when the level expected at resumption from the proxies that kept
recording is set against the level observed?

% ===========================================================================
\section{The record and the three regressor sets}
\label{sec:record}

The study window is the whole station~02 record, from 26~July 2018 to the end
of the archive in August 2026, on the instrument's native grid of one reading
every twenty minutes: 212,251 slots. Unlike Study~04, which worked on the stretch
after the long outage of 2022 to 2023, this study uses every usable annual
cycle, because an annual term cannot be identified on three broken years, and
a trend estimated on such a window disagreed with itself in sign
(Section~\ref{sec:method}, D1).

The response is joined by three regressor sets, each mapping the same three
roles, air temperature, relative humidity and solar radiation, to a different
source (D2). The on-structure set reads air temperature and humidity from the
wall's own package, joined across the two instrument eras; the station set
reads the town's ground station; the ERA5 set reads the reanalysis. The
on-structure set borrows the station's radiation, because the wall's
pyranometer did not exist before February 2025 and Study~01 condemned 161 of
its days after that; every table that reports the on-structure set carries
that borrowing. The two external sources are hourly or half-hourly and are
brought onto the twenty-minute grid by linear interpolation between
consecutive native observations, never across a longer gap, and the ERA5
radiation, an accumulation over the preceding hour, is centred on the
half-hour before it is interpolated (D13). Regressor dropouts of at most two
hours are then filled by linear interpolation and flagged; the target is never
filled.

Table~\ref{tab:coverage} gives what each set actually holds over the window.
The inclination returns an accepted value in 67.7\,\% of the slots. The
on-structure air temperature and humidity reach 71.1\,\%, with about two per
cent of their accepted slots filled across short dropouts; the station's
channels reach 91.7 to 94.7\,\%, with under one per cent filled; ERA5 covers
99.2\,\% of the window with nothing to fill. The radiation of the on-structure
and station sets is the same column and has the same coverage.

\begin{table}[H]
\centering
\caption{Accepted and filled slots and coverage per regressor set and role over
the study window, on the twenty-minute grid, with the target as the last row.
The on-structure set's radiation is the station's column, borrowed. Read from
\texttt{GM\_01\_window\_coverage.csv}.}
\label{tab:coverage}
\begin{tabular}{llrrr}
\toprule
Set & Role & Accepted slots & Filled slots & Coverage \\
\midrule
\input{../outputs/GM_01_body.tex}\\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:gapclasses} shows how the target's missing time is distributed.
Of the 3,302 gaps in the accepted inclination, 2,616 last an hour or less and
595 between one and six hours; together they hold 2,679 of the 22,819 missing
hours. Nine gaps longer than seven days hold 19,234 hours, 84\,\% of the
missing time. These are gaps in the accepted values rather than in the raw
archive, and they are therefore longer than the outages the data-quality
report lists: two interruptions of three weeks and one of eight in the autumn
of 2018 and the first half of 2019, when the legacy instrument's readings were
condemned; 57 days in the summer of 2020, around an archive outage of eleven;
a single gap of 437.6 days from 9~April 2022 to 21~June 2023, in which the two
outages of 2022 and the condemned weeks before and between them merge; and the
outages of 2024, 2025 and 2026, of 41.5, 42.7, 17.8 and 104 days. Nothing is
written into any of them. They are what the rolling expectation must be
refitted around, and the ones that fall in the monitored period are the
hypotheses that Section~\ref{sec:outages} tests.

\begin{table}[H]
\centering
\caption{The target's missing time by gap duration: how many gaps of each class
the accepted inclination has, and how many hours they hold. Read from
\texttt{GM\_02\_gap\_inventory.csv}.}
\label{tab:gapclasses}
\begin{tabular}{lrr}
\toprule
Gap class & Gaps & Hours \\
\midrule
\input{../outputs/GM_02_body.tex}\\
\bottomrule
\end{tabular}
\end{table}

Before anything was joined, each radiation source's clock was checked with
Study~02's two-sided test, on the hourly grid the test was designed for
(Table~\ref{tab:clock}): the offset of the daily maximum from computed solar
noon, and the lag of the cross-correlation against ERA5. The station and ERA5
pass both tests, with solar offsets of 0.11 and 0.44 hours and no
cross-correlation lag. ERA5's larger offset is the signature of its hourly
accumulation, which is stamped at the end of the hour it describes and which
D13 centres before interpolation. The on-structure pyranometer fails the
agreement test over its 215 certified days, its solar offset of $-0.22$ hours
sitting against a cross-correlation lag of one hour. No regressor set consumes
that channel in the main line; it enters only the current-era ladder of D4,
where its clock is part of what the ladder judges.

\begin{table}[H]
\centering
\caption{Study~02's clock check on each radiation source, on the hourly grid:
the days with a usable solar maximum, the offset of that maximum from computed
solar noon with its interquartile range, the cross-correlation lag against ERA5
with its correlation, and whether the two tests agree. Read from
\texttt{GM\_03\_clock\_check.csv}.}
\label{tab:clock}
\begin{tabular}{llrrrrrr}
\toprule
Source & Channel & Days & Solar offset [h] & IQR [h] & Lag [h] & $r$ & Agree \\
\midrule
\input{../outputs/GM_03_body.tex}\\
\bottomrule
\end{tabular}
\end{table}

Figure~\ref{fig:regressor-sets} draws the record and the three sets on one
clock, one record per panel, with the gaps left as gaps.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{GM_F01_regressor_sets.png}
\caption{The record and the three regressor sets, 2018 to 2026, on the
twenty-minute grid, one record per panel. Top: the compensated, once-anchored,
spike-cleaned inclination, its gaps left as gaps. Below: air temperature,
relative humidity and global horizontal radiation from each source, the panels
of one role sharing their vertical scale. The station's radiation is drawn once
and labelled for both the station set and the on-structure set that borrows
it; the wall's own pyranometer does not enter the main line.}
\label{fig:regressor-sets}
\end{figure}

% ===========================================================================
\section{Method}
\label{sec:method}

Each subsection states one design decision, the evidence for it, and the
alternative it rejects, in the order of the design document. Decisions whose
results arrive in later phases, D6 onward, are stated here and reported in
their own sections.

\subsection{D1 · The whole record, 2018 to the archive end}

Study~04's annual term was not identifiable on 3.2 cycles with a 103-day hole,
and its trend disagreed with itself in sign across the thirds of its window:
$+38.6$, $+4.8$ and $+45.5$\,mdeg per year against $-2.77$ for the window as a
whole. The whole record holds five usable annual cycles, 2019, 2020, 2021, 2024
and 2025, compensated and anchored once by Study~01. The instrument change of
21~February 2025 is not modelled: the recorded channel is continuous across it
to $+1.58$\,mdeg on thirty-day means, and the compensated gain is stable across
both eras. The rejected alternative is a per-era offset, which would plant an
arithmetic step the instruments did not record.

\subsection{D2 · Three regressor sets through one model specification}

One specification is fitted three times, once per regressor set. The
on-structure set gives the site result, the station set the best external
proxy, and the ERA5 set the transferable arm. The paper narrates two and
tables all three. The on-structure set borrows the station's radiation, as
Section~\ref{sec:record} states. Wall temperature and on-structure radiation
stay out of the main line, since they cover 17.6\,\% and 18.8\,\% of the
window, and are tested on their own window in D4. The rejected alternative, a
single set, would not separate what the site can achieve from what a
deployment without on-structure instruments could.

\subsection{D3 · Study~03's operator is applied outside the model; radiation is global horizontal in every set}

Air temperature enters instantaneously. Radiation enters delayed by one hour,
the diurnal-band optimum Study~03 measured for all three sources. Both pass
through the library's one delay-and-filter operator, and no driver is given a
time constant from the level band, where Study~03 showed such constants pin
against the grid. Global horizontal irradiance is used in every set, so that
the radiation gain is comparable across sets and matches the variable
Study~03 screened. ERA5's skin temperature is not loaded: the radiative
temperature of a grid cell several kilometres across, averaged over fields,
roads and roofs, carries nothing about one stone face that the air temperature
and radiation do not already carry. The rejected alternative, letting Model~A
learn the delay through lagged regressors, is the job of Model~B in D9, where
the learned response is confronted with the imposed one rather than replacing
it.

\subsection{D4 · Wall temperature and on-structure radiation, tested as a ladder on the current era}

On the window from 21~February 2025 onward, the same specification is refitted
rung by rung: the on-structure set as in the main line; on-structure radiation
replacing the station's, on the certified days; the wall temperature added at
zero delay with a four-hour time constant, the deployable form; and, as a
diagnostic row only, the wall temperature at its measured lead of two hours,
which is never used for the expectation. Each rung reports held-out error with
a paired block-bootstrap increment, variance share, the learned gain beside
Study~03's, conformal coverage and width, and residual autocorrelation. The
verdict is one sentence per channel on whether a deployment should keep it.
The rejected alternative, carrying both channels in the main line, would cost
four fifths of the window.

\subsection{D5 · The trend stays on in the monitoring fit}

Study~04 removed the trend from its monitoring fit so that drift would remain
in the residual. The consequence was a residual with lag-one autocorrelation
of 0.995, a control limit swept out to 14.75 standard deviations, a 288-day
alarm that said only that the wall had moved since calibration, and a detector
blind to every slow mechanism. Here the expectation comes from a rolling refit
every thirty days with the trend on; over thirty days the trend's extrapolation
error is a fraction of a millidegree at the measured rates, and the residual it
leaves is stationary enough to chart. Drift is detected by the slow chart of
D10 on daily means, and by the sequence of refit trend slopes. Component shares,
gains and the parameter figures come from a single attribution fit over the
full training window.

\subsection{D6 · Harmonic diagnostics before modelling}

The Fourier orders of the seasonal terms and the weight curve of the
conditional daily term are measured before any model is fitted, on two series:
the target, and the residual of a plain least-squares regression of the target
on the on-structure drivers, which is what the seasonal terms will actually
have to explain. A Lomb--Scargle scan from twelve hours to nine hundred days
certifies the periods; the daily cycle's amplitude and phase, fitted day by
day, are then fitted against day of year with an annual Fourier series whose
order is chosen on held-out years; and the singular values of the
daily-by-annual surface say how many weighted daily shapes it takes to
reproduce it. Every order and weight used afterwards traces to that table. The
results are reported in Section~\ref{sec:seasonality}.

\subsection{D7 · Model~A, an additive grey-box without autoregression}

Model~A is a linear trend with changepoints placed on covered time, an annual
term and a daily term as Fourier series of the orders D6 measured, no weekly
term, the three regressors of one set as future regressors, quantile regression
at 0.05 and 0.95, and no autoregression, with the library's imputation switched
off. The daily cycle is fitted as two shapes, a midsummer and a midwinter form,
blended by two weights that vary only with the day of year and sum to one, so
that the daily cycle's amplitude and phase modulate through the year without a
boundary day at which the shape steps. The weight curve is the measured annual
modulation of D6, normalised to the unit interval, with a cosine peaking in
mid-July as the documented fallback. The conditional term is kept only if it
improves the held-out error or carries more than one per cent of the variance,
and four boolean seasons are the stated fallback if the smooth weights fail to
fit. Training histories shorter than two full annual cycles are not allowed to
speak.

\subsection{D8 · Uncertainty the library's way, calibrated on a rolling window}

Quantile regression inside the model gives a nominal ninety per cent interval;
split conformal prediction then calibrates it. Study~04 calibrated once, on a
stretch that ended eleven months before the record it covered, and reached
67.7\,\% coverage. Here each refit calibrates on the most recent six months of
its own out-of-sample residuals, so the interval is never calibrated on a
quieter era than the one it covers. Coverage, width, Winkler score and pinball
loss are reported per set and per days since refit. The stability of the
decomposition's components is measured across the library's own
chronological folds, each refitted from scratch on the record up to its
held-out stretch: a gain or a seasonal amplitude is reported as a finding
only if it keeps its sign and its order of magnitude in every fold, and a
component that changes sign between folds is not.

\subsection{D9 · Model~B, a learned impulse response on the on-structure set}

The same specification without any autoregression on the target carries air
temperature and the station's radiation as lagged regressors with thirty-six
lags at twenty minutes, twelve hours of history per driver, at the resolution
Study~03's scan could not reach. The learned weight per lag is the impulse
response; from it an effective delay and a time constant are read and set
beside Study~03's operator for each driver. Agreement validates the operator
imposed in Model~A; disagreement is reported, and Model~A is not re-tuned to
hide it. Phase~0 verified that the library accepts lagged regressors without
autoregression on the target, so the hourly fallback the design provided for is
not needed.

\subsection{D10 · Three charts, one per damage mechanism and time scale}

Reference statistics come from a fixed window before the 2022 outages and
before the instrument change, planned as 2019 to 2021 and taken as
21~November 2020 to 31~December 2021, because the rolling residual the charts
run on exists only from its first refit origin (Section~\ref{sec:monitor});
the monitored record runs from January 2022. All three charts run on Model~A's
rolling residual for each set.
The fast chart applies an exponentially weighted moving average and a CUSUM to
the residual's prewhitened innovations at twenty minutes, for steps, spikes
and instrument faults, at a budget of ninety watched days per false alarm. The
daily chart applies the same pair to the amplitude and phase of a 24-hour
harmonic fitted to each day's residual, for amplitude growth and phase change,
at the same budget. The slow chart applies a CUSUM with a small reference value
to the daily mean residual, for drift, at a budget of one year; the design
placed the sequence of refit trend slopes beside it, and that companion was
not built (Section~\ref{sec:monitor}). Every fast alarm is cross-checked against the
air temperature, humidity and battery channels in the same slot, so that the
episode table carries an attribution: environment, instrument, or unattributed.

\subsection{D11 · Detectability by mechanism, with injections sized by the wall's daily response}

Study~04's three injection shapes, amplitude growth, phase change and drift,
are kept with two corrections. The phase injection is sized by the wall's
fitted daily response on the injection date rather than by the leftover daily
seasonal term, which on the measured numbers means about 5.6\,mdeg for a
two-hour shift where Study~04 injected 1.07. Injections are placed at several
dates across the seasons, so that the detectability field is monotone rather
than an artefact of one injection point. Each mechanism is scored on the chart
built for it, with the other two reported as well, and an alarm counts only
where the uncontaminated record is silent within the response window.

\subsection{D12 · Outages as hypotheses, never as data}

For each whole-day outage of seven days or more in the monitored period, the
model refitted on the data up to the outage predicts the level at resumption
from the station and ERA5 sets, which kept recording. The observed mean over
the first seven days after resumption, with the restart transient excluded, is
compared with the expected level and its interval, giving a level shift with
an interval and a verdict per outage and per proxy set; the interval is the
quantile band of the fit made before the outage rather than the conformal
interval the design named, for the reason Section~\ref{sec:outages} gives. Nothing is
written into the gap: an imputed value may serve as history or as hypothesis,
never as evidence. Section~\ref{sec:record} showed that the accepted target's
gaps are longer than the archive's outages; which intervals are bridged is
stated in Section~\ref{sec:outages}.

\subsection{D13 · The target is never filled; regressor dust is}

Rows with a missing target are left out of every fit. Regressor gaps of at most
two hours are filled by linear interpolation and flagged in a provenance
column, because a regressor is a measured, smooth driver and not the thing
being judged; longer regressor gaps leave the row out of the fit for that set
only. Proxies are brought to the twenty-minute grid by linear interpolation
between consecutive native observations, never across a longer gap, with the
ERA5 radiation centred on the half-hour before interpolation. Each source's
clock is checked before anything is joined, as Section~\ref{sec:record}
reports.

\subsection{D14 · Every native plot type, redrawn in the project's graphical language}

The library's native plots run in the notebook as diagnostics. The report shows
the same content redrawn by the project's own figure functions under its
binding graphical rules, each redraw reading its data through the library's
public methods, the decomposed prediction, the trend and seasonal components,
the lagged-regressor weights and the conformal output columns, through one
extractor per plot type, and each extractor is tested against the numbers the
native plot draws. Phase~0 found that this library version's matplotlib
backend returns no figure object from its parameter plot; nothing in the
design depends on it.

% ===========================================================================
\section{Trend}
\label{sec:trend}

Model~A was fitted once per regressor set for attribution, on the first four
fifths of each set's rows, with the last fifth held out as the library's own
validation split (D5, D7). On the on-structure set the training window runs
from 21~November 2018, the first slot at which all three drivers are present,
to 11~March 2025, and holds 109,259 rows; the station and ERA5 sets hold
106,849 and 113,955. Twelve changepoints were placed on covered time, so that
none falls inside a gap, and the trend's regularisation was chosen on the
held-out tail before the attribution fits were made.

\subsection{The regularisation sweep}

Table~\ref{tab:trend-reg} gives the sweep. Without regularisation the held-out
mean absolute error is 17.7\,mdeg. Every positive candidate gives the same
held-out error, 122.347\,mdeg on each of the four: the library's penalty on the changepoint
deltas, at any of the four strengths tried, drives every delta to zero, and
the trend collapses to a single straight line through six and a half years of
a record that is not straight. The candidates are in the wrapper's own scale;
NeuralProphet multiplies a positive value by 0.001 before it reaches the loss.
The study therefore keeps the unregularised trend, and the parameter cell
records \texttt{TREND\_REG = 0.0} with this table as its reason.

\begin{table}[H]
\centering
\caption{The trend-regularisation sweep on the on-structure set: the held-out
mean absolute error of the last fifth of the rows for each candidate, and the
one kept. Read from \texttt{GM\_05c\_trend\_reg\_sweep.csv}.}
\label{tab:trend-reg}
\begin{tabular}{rrl}
\toprule
Trend regularisation & Held-out MAE [mdeg] & Kept \\
\midrule
\input{../outputs/GM_05c_body.tex}\\
\bottomrule
\end{tabular}
\end{table}

\subsection{The trend on covered time}

Figure~\ref{fig:trend} draws the fitted trend of the on-structure set and the
rate of each segment between changepoints; Table~\ref{tab:trend-rates} lists
the rates. The trend carries 60.1\,\% of the fitted variance on the
on-structure set, 60.7\,\% on the station set and 64.1\,\% on ERA5
(Table~\ref{tab:shares}), with a peak-to-peak of 116.4, 119.9 and
139.5\,mdeg: over six years the level of the compensated inclination moved by
more than a hundred millidegrees net of air temperature, humidity, radiation
and the annual cycle, and that movement is what the trend holds.

Its shape is not a drift. The level rose through 2019 and 2020, at rates
between 17 and 104\,mdeg per year over segments of four to six months, to a
maximum at the changepoint of 19~November 2020; it then fell through 2021 at
29 to 91\,mdeg per year. Across the long gap of 2022--2023 the segment from
5~December 2021 to 26~June 2023 has a rate of $-2.2$\,mdeg per year, which is
the straight line between the last data before the gap and the first after
it, not a measurement of anything inside it. From mid-2023 the level fell
again, at 89 and 49\,mdeg per year, to a minimum at the changepoint of
31~March 2024, and rose by 28\,mdeg per year through the rest of 2024 before
flattening, at 3\,mdeg per year, in the first months of 2025.

Two readings of this trend must be kept apart. As a component of the
decomposition it is the slow part of the record after the drivers and the
annual term, and its rates are the wall's net movement on the scale of months.
As evidence about the structure it says nothing on its own: a rate of
100\,mdeg per year sustained for five months and then reversed is the
signature of a slow, reversible process, and whether that process is
structural or a thermal memory of the masonry that the compensation and the
instantaneous drivers do not carry is what the monitor of
Section~\ref{sec:monitor} and the current-era ladder of
Section~\ref{sec:ladder} are built to separate. The mean rate over a fold is
not a stable quantity: in the chronological folds of
Table~\ref{tab:stability} it changes sign between folds on the on-structure
and station sets, and on ERA5, where it stays positive, it runs from 4.4 to
20.2\,mdeg per year, because each fold's training window ends at a different
point of the same reversing record. On the first two sets the rule of
Section~\ref{sec:method} excludes it as a finding; on ERA5 it is reported for
what it is, the mean of a reversing record over whatever stretch a fold
happens to cover, not a rate of the wall. The segment rates of
Table~\ref{tab:trend-rates} are findings.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{GM_F04_trend.png}
\caption{The trend of Model~A on the on-structure set over the training
window, 2018 to 2025. Top: the fitted trend on covered time, with the twelve
changepoints marked. Bottom: the rate of each segment between changepoints,
in millidegrees per year. The segment that spans the 2022--2023 gap is the
straight line between the data on either side of it.}
\label{fig:trend}
\end{figure}

\begin{table}[H]
\centering
\caption{The rate of the fitted trend on each segment between consecutive
changepoints of the on-structure fit, from the trend's value at the segment's
ends. Read from \texttt{GM\_04d\_trend\_rates.csv}.}
\label{tab:trend-rates}
\begin{tabular}{llr}
\toprule
From & To & Rate [mdeg/yr] \\
\midrule
\input{../outputs/GM_04d_body.tex}\\
\bottomrule
\end{tabular}
\end{table}

% ===========================================================================
\section{Seasonality}
\label{sec:seasonality}

\subsection{The harmonic diagnostic}
\label{sec:harmonic-diagnostic}

The Fourier orders of the seasonal terms and the weight curve of the
conditional daily term were fixed before any model was fitted (D6). Two series
were examined: the target itself, and the residual of an ordinary
least-squares regression of the target on the three drivers of the
on-structure set, which is what the seasonal terms will actually have to
explain once the regressors have taken their share. That plain fit is a
diagnostic device, not a model of the wall; on the levels it attributes
the bulk of the daily and annual swing to air temperature; its coefficients are not
reported here, and its residual carries whatever the drivers, entering
instantaneously, do not.

Each series was scanned with a Lomb--Scargle periodogram, which takes the
gaps as they are, on two bands ranked separately: from 0.4 to 3 days for the
daily cycle and its harmonics, and from 30 to 900 days for the annual cycle
and its harmonics (Table~\ref{tab:certified-periods}). The bands are kept apart
because the sub-daily peaks carry a small share of the variance, about
1.1\,\% for the target's daily cycle and 0.1\,\% for the residual's, and a
single ranking fills every rank with the long-period continuum before it
reaches them. On the target the long band certifies the annual cycle at 364.2
days with a normalised power of 0.42, and the semi-annual cycle at 181.7 days
with 0.035, inside its 11-day resolution of half a year; the remaining
long-band entries at 311, 437 and 600 days are the leakage of the multi-year
drift and of the record's gaps rather than cycles. On the residual the annual
cycle survives at 352.7 days, within its 44-day resolution of a year, at a
power of 0.061, and no semi-annual peak does: the drivers, entering
instantaneously, take that component with them. The 900-day entry at the top
of the residual's long band is the grid's edge, a third of the record, and is
not a period. In the short band both series certify the daily cycle at 1.000
days and its 12-hour companion at 0.500 days; the entries at 0.9996, 1.0005
and 1.0013 days are the daily peak's sidelobes, spaced by the record's
resolution and listed because they fall just outside the width within which
the scan suppresses a peak's neighbours.

\begin{table}[H]
\centering
\caption{Periods certified by the Lomb--Scargle scan on the target and on the
regression residual, each band ranked against its own continuum. The
resolution is the width around a period that this record cannot tell from the
period itself. Read from \texttt{GM\_04\_harmonic\_diagnostics.csv}.}
\label{tab:certified-periods}
\begin{tabular}{llrrr}
\toprule
Series & Band & Rank & Period [days] & Power \\
\midrule
\input{../outputs/GM_04_body.tex}\\
\bottomrule
\end{tabular}
\end{table}

For every day with at least 60 of its 72 slots, a 24-hour harmonic and its
12-hour companion were fitted to the diurnal band of each series, giving the
daily cycle's amplitude and the hour of its maximum over eight years
(Figure~\ref{fig:harmonic-diagnostics}). Each of those two daily series was
then fitted against day of year with an annual Fourier series, the order
chosen on held-out years and a higher order accepted only where it lowered
the held-out error by at least one per cent
(Table~\ref{tab:annual-modulation}). On the target the fitted daily amplitude
runs from a minimum of 2.8\,mdeg in early December (day 342) to a maximum of
17.1\,mdeg in late July (day 200), a six-fold swing; on the residual it runs
from 2.2\,mdeg in mid-April (day 102) to 3.3\,mdeg in late July (day 206), a
swing of one and a half. The two share the summer peak and not the trough:
once air temperature has been removed, the residual's daily cycle is smallest
in spring rather than in winter. Both fits take order two, and the residual's
fit, the one the conditional daily
term consumes, has coefficients $(2.620, -0.292, -0.135, 0.265, 0.268)$ for
the constant, the first cosine and sine and the second cosine and sine over a
period of 365.25 days; normalised to the unit interval it peaks on day 206.
That the residual's daily cycle modulates weakly once air temperature has
been removed is itself a finding: most of the annual modulation of the daily
swing rides on the driver, and the conditional term of D7 has less to carry
than the target's picture suggests. Whether the two weighted shapes still
earn their place is decided by the held-out comparison reported in the second
half of this section.

\begin{table}[H]
\centering
\caption{Annual modulation of the daily cycle's amplitude and hour of maximum,
per series: the leave-one-year-out error of each candidate Fourier order and
the order chosen. Read from \texttt{GM\_04\_harmonic\_diagnostics.csv}.}
\label{tab:annual-modulation}
\begin{tabular}{llrrl}
\toprule
Series & Statistic & Order & Held-out MSE & Chosen \\
\midrule
\input{../outputs/GM_04b_body.tex}\\
\bottomrule
\end{tabular}
\end{table}

The hour of the daily maximum is a circular quantity, and it was recentred on
each series' circular mean before fitting, so that the two labels of midnight
did not pull the fit apart. On the target the maximum sits near midnight
throughout the year and its annual fit takes order one, with a held-out error
that order two does not improve: the inclination's daily cycle keeps its
timing, the wall tipping farthest towards the mountain in the afternoon and
recovering overnight. On the residual the maximum sits near 17\,h in winter
and moves to about 23\,h in July, an excursion of about six hours that order
two fits better than order one by three per cent held out. What the drivers
leave behind therefore changes its shape as well as its size through the year,
which is the case for two weighted daily shapes rather than one scaled shape.

The diurnal band of the residual, binned by week of the year and slot of the
day, is a surface whose singular values say how many weighted daily shapes it
takes to reproduce it (Table~\ref{tab:surface-rank}). The first component
carries 69\,\% of its variance and the second 24\,\%, 92.7\,\% together; a
third adds 5\,\%. Two blended shapes therefore reproduce the surface to within
a few per cent, which licenses the two conditions of D7 and does not call for
a third.

\begin{table}[H]
\centering
\caption{Singular-value decomposition of the residual's daily-by-annual
surface: share of variance per component and cumulative share. Read from
\texttt{GM\_04\_harmonic\_diagnostics.csv}.}
\label{tab:surface-rank}
\begin{tabular}{rrr}
\toprule
Component & Share & Cumulative \\
\midrule
\input{../outputs/GM_04c_body.tex}\\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{GM_F02_harmonic_diagnostics.png}
\caption{The harmonic diagnostic. Top left: the periods the scan certifies on
the target (solid) and on the regression residual (dashed), on a logarithmic
period axis, with the half-day, day, half-year and year marked. Top right:
the daily amplitude by day of year with its annual fit. Bottom left: the hour
of the daily maximum, recentred on its circular mean, with its annual fit.
Bottom right: the share of variance of the leading components of the
daily-by-annual surface.}
\label{fig:harmonic-diagnostics}
\end{figure}

Three parameters follow from the diagnostic and are declared in the notebook's
parameter cell with a pointer to \texttt{GM\_04}. The yearly term takes Fourier
order one, because the residual certifies the annual cycle and no semi-annual
one. The daily term takes order two, because both series certify the 12-hour
companion of the daily cycle, the signature of a daily response that heats
faster than it cools. The weight curve of the conditional daily term is the
residual's order-two annual fit of the daily amplitude, normalised to the unit
interval and peaking on day 206, and the two weighted shapes go forward to
Phase~2's held-out test, since the first two components of the surface carry
92.7\,\% of its variance.

\subsection{The fitted terms, and the conditional daily term's test}
\label{sec:seasonal-fitted}

Figure~\ref{fig:seasonality} draws the two seasonal terms Model~A fitted on
the on-structure set. The yearly term, of Fourier order one, has a
peak-to-peak of 44.4\,mdeg and carries 12.2\,\% of the fitted variance
(Table~\ref{tab:shares}); on the station and ERA5 sets it is 51.9 and
49.3\,mdeg and 16.8 and 14.6\,\%. On the on-structure set it reaches its
maximum of $+22.2$\,mdeg on 21~March, day 80, and its minimum of
$-22.2$\,mdeg on 20~September, day 263 (\texttt{GM\_05d}): the term peaks at
the equinoxes, in quadrature with the annual cycle of air temperature, which
peaks near the solstices. That is the shape a delayed annual response leaves
when its driver enters instantaneously, since the first-order correction for
a lag is proportional to the driver's rate of change, largest when the
season turns; the yearly term is the part of the wall's seasonal response
that follows the air with a delay the instantaneous driver cannot supply.
The delay itself is not estimated here. The daily term, of order two, is
small, as D7 anticipated: a peak-to-peak of 3.9\,mdeg and 0.1\,\% of the
variance on the on-structure set, 8.2\,mdeg and 0.4\,\% on the station set,
13.4\,mdeg and 1.0\,\% on ERA5, larger where the driver is measured farther
from the wall, since whatever the driver does not explain of the daily cycle
is left for the term to hold. On the on-structure set it rises to
$+2.3$\,mdeg at 17:00\,UTC and falls to $-1.6$\,mdeg at 08:20\,UTC
(\texttt{GM\_05d}), the evening and the morning, on either side of the
afternoon maximum of the air temperature that carries the rest of the daily
swing.

The conditional daily term did not earn its place
(Table~\ref{tab:conditional}). Fitted as two shapes weighted by the measured
annual curve, it raised the held-out error from 17.745 to 18.177\,mdeg and
carried 0.12\,\% of the variance against the plain term's 0.09\,\%, under the
one per cent D7 set as the alternative criterion. Every fit downstream of
this comparison therefore uses the plain daily term of order two, and the two
shapes the design would have drawn at the solstices and equinoxes collapse to
one curve, which is the one Figure~\ref{fig:seasonality} shows. The outcome
is consistent with the diagnostic: once air temperature has been removed, the
daily cycle's annual modulation is weak, 2.20 to 3.32\,mdeg over the year
(Table~\ref{tab:annual-modulation}), and a term that varies with the season
has almost nothing to modulate. D7's further fallback, four boolean seasons,
is not reached: the smooth weights fitted; they were not worth their
parameters.

\begin{table}[H]
\centering
\caption{The plain daily term against the conditional one, on the
on-structure set: held-out mean absolute error and the share of variance the
daily term carries in each fit. Read from \texttt{GM\_05b\_conditional\_test.csv}.}
\label{tab:conditional}
\begin{tabular}{lrr}
\toprule
Daily term & Held-out MAE [mdeg] & Share of variance \\
\midrule
\input{../outputs/GM_05b_body.tex}\\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{GM_F05_seasonality.png}
\caption{The seasonal terms of Model~A on the on-structure set. Left: the
yearly term over one synthetic year, by day of year. Right: the daily term
over one synthetic day, by hour of day in UTC. With the conditional term
rejected, the daily curve is the same on every day of the year.}
\label{fig:seasonality}
\end{figure}

% ===========================================================================
\section{Regressors and gains}
\label{sec:regressors}

Table~\ref{tab:shares} gives what each set's record is made of, and
Figure~\ref{fig:decomposition} draws the decomposition of the on-structure set
over the training window. The picture is the same on all three sets. The trend
holds three fifths of the fitted variance (60.1, 60.7 and 64.1\,\%); air
temperature holds the next largest share, 24.1\,\% on the on-structure set,
18.3\,\% on the station set and 15.6\,\% on ERA5, with a peak-to-peak
contribution of 115.4, 100.9 and 105.0\,mdeg; the yearly term holds 12 to
17\,\%; the residual 3.1, 3.7 and 4.2\,\%; and humidity, the daily term and
radiation together hold 0.42 and 0.49\,\% on the two nearer sets and 1.51\,\%
on ERA5. Net of its slow movement, the record is a thermometer: the contribution
of air temperature alone swings by more than a hundred millidegrees.

\begin{table}[H]
\centering
\caption{The share of the fitted variance each additive component carries and
its peak-to-peak contribution over the training window, per regressor set,
ordered by share. The on-structure set's radiation is the station's column,
borrowed. Read from \texttt{GM\_05\_component\_shares.csv}.}
\label{tab:shares}
\begin{tabular}{llrr}
\toprule
Set & Component & Share & Peak-to-peak [mdeg] \\
\midrule
\input{../outputs/GM_05_body.tex}\\
\bottomrule
\end{tabular}
\end{table}

\subsection{The gains against Study~03}

Table~\ref{tab:gains} and Figure~\ref{fig:gains} put each learned gain beside
Study~03's. NeuralProphet's additive future regressor is linear in the driver,
so the gain is the slope of the component against the driver and is read off
exactly; the coefficient of determination of that reading is one by
construction and is not tabulated. On the on-structure set the air-temperature
gain is $-2.639$\,mdeg per degree against Study~03's $-2.79$, within
5.4\,\% of it and inside the month-to-month range of $-3.63$ to $-1.88$ that
Study~03 measured over 35 months. This is the agreement Checkpoint~2 required
before the study could go on: an additive model with a trend, an annual term
and three instantaneous drivers, fitted on six years of levels, recovers the
coupling that Study~03 measured by a different method on the diurnal band.
The station and ERA5 sets learn $-2.402$ and $-2.295$ against Study~03's
$-2.23$ and $-2.04$, larger in magnitude by 7.7 and 12.5\,\%; a driver
measured farther from the wall is smoother than the wall's own air, and a fit
on levels compensates with a larger slope where Study~03's band-limited fit
did not have to.

Relative humidity, for which Study~03 reports a positive correlation and no
gain, learns $+0.108$, $+0.092$ and $+0.140$\,mdeg per percentage point:
positive on every set, small, and worth 8.8 to 11.5\,mdeg peak-to-peak over
the humidity range the record spans. Study~03 read the correlation as the
inverse of the temperature cycle rather than as a mechanism, and nothing here
contradicts that. Radiation is the one driver the model does not recover.
Study~03 measured $-0.035$\,mdeg per W\,m$^{-2}$ for the station's radiation
and $-0.026$ for ERA5's on the diurnal band; here the learned gains are
$+0.006$ on the on-structure set, $-0.001$ on the station set and $-0.011$
on ERA5, with peak-to-peak contributions of 5.8, 0.6 and 10.0\,mdeg, and on
the on-structure set the sign is the wrong one. The discrepancy is less a
contradiction of Study~03 than a statement about what a fit on levels can
separate: radiation, delayed by an hour, moves in step with the air
temperature that heats with it, and its marginal contribution once
temperature and the daily term have taken theirs is close to zero. Study~03
asked what radiation does to the diurnal swing; this fit asks what it adds to
the level beyond air temperature, and answers that it adds little.
Section~\ref{sec:impulse} returns to the question with a learned impulse
response, and the ladder of Section~\ref{sec:ladder} asks it again of the
wall's own pyranometer.

\begin{table}[H]
\centering
\caption{The gain each future regressor learned, per set, beside Study~03's
diurnal-band measurement where one exists. Units: millidegrees per degree
Celsius for air temperature, per percentage point for relative humidity and
per W\,m$^{-2}$ for radiation. Read from \texttt{GM\_06\_learned\_gains.csv}.}
\label{tab:gains}
\begin{tabular}{llrr}
\toprule
Set & Driver & Learned gain & Study~03 \\
\midrule
\input{../outputs/GM_06_body.tex}\\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{GM_F06b_gains.png}
\caption{The learned gain per set and driver (solid) beside Study~03's
measurement (light), in millidegrees per unit of the driver. Study~03 has no
gain for relative humidity.}
\label{fig:gains}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{GM_F06_decomposition.png}
\caption{The additive decomposition of the on-structure set over the training
window: the trend, the yearly and daily terms, the contribution of each
driver and the residual, on one clock, with the lines broken across the
gaps.}
\label{fig:decomposition}
\end{figure}

\subsection{Stability across folds}

Table~\ref{tab:stability} reports the gain, the yearly amplitude and the mean
trend rate on each of NeuralProphet's five chronological folds, each refitted
from scratch on the record up to its own held-out tenth. The air-temperature
gain keeps its sign in every fold and stays within $-2.45$ to $-2.70$ on the
on-structure set, $-2.28$ to $-2.50$ on the station set and $-2.21$ to
$-2.34$ on ERA5; the yearly peak-to-peak stays within 39 to 45, 44 to 52 and
45 to 50\,mdeg. Both are findings by the rule of Section~\ref{sec:method}. The
mean trend rate is not a finding about the wall: it changes sign between
folds on the on-structure and station sets and, on ERA5, where it keeps its
sign, runs from 4.4 to 20.2\,mdeg per year, because each fold's window ends
at a different point of a record whose slow movement reverses; it is the mean
of that record over the stretch a fold covers, not a rate of the structure. The held-out error of the folds, from
7.8 to 100.7\,mdeg on the on-structure set, measures how far a trend
extrapolated over a tenth of the set's rows, 13,657 slots of twenty minutes
or about 190 days, wanders from the level the wall actually reached.
That number is the case for the thirty-day refit of D5, which
Section~\ref{sec:uncertainty} scores.

\begin{table}[H]
\centering
\caption{Component stability across NeuralProphet's five chronological folds,
per set: the air-temperature gain, the yearly term's peak-to-peak, the mean
trend rate and the held-out mean absolute error of each fold. Read from
\texttt{GM\_07\_component\_stability.csv}.}
\label{tab:stability}
{\small\setlength{\tabcolsep}{4.5pt}
\begin{tabular}{lrrrrr}
\toprule
Set & Fold & Gain [mdeg/$^{\circ}$C] & Yearly p--p [mdeg] & Trend rate [mdeg/yr] & Held-out MAE [mdeg] \\
\midrule
\input{../outputs/GM_07_body.tex}\\
\bottomrule
\end{tabular}}
\end{table}

\subsection{What the residual still holds}

Table~\ref{tab:residual} gives the residual's scale and whiteness. Its
standard deviation is 7.97\,mdeg on the on-structure set, 8.65 on the station
set and 9.47 on ERA5, with median absolute deviations of 4.76, 5.27 and 5.39:
the nearer the driver, the less is left. It is not white. The Ljung--Box test
rejects independence at lags of one slot, one day and three days on every set
with a p-value that rounds to zero, and the bottom panel of
Figure~\ref{fig:decomposition} shows why: the residual wanders over weeks,
with a peak-to-peak of 60.6\,mdeg on the on-structure set
(Table~\ref{tab:shares}). The component that would absorb this is the
record's own memory, an autoregressive term, and D7 leaves it out on purpose,
because with it the decomposition stops reading as physics. The memory is
handed instead to the two places built for it: the rolling refit of D5, which
re-anchors the expectation every thirty days so that the wandering never
accumulates, and the prewhitening of D10, which removes the residual's
lag-one dependence before a chart is drawn on it.

Table~\ref{tab:residual-periods} names the periodic structure the residual
still carries, scanned band by band as the diagnostic of
Section~\ref{sec:harmonic-diagnostic} was. In the short band the daily line
survives on every set, at normalised powers of 0.004 to 0.006, and it
survives split in two: at 0.9972 and 1.0027 days on the on-structure and
station sets, the first annual sidebands of the daily cycle,
$1/(1 \pm 1/365.25)$ days, and at 0.9946 and 1.0027 days on ERA5, whose lower
line is the second sideband, $1/(1 + 2/365.25)$ days. These are the
seasonally modulated part of the daily cycle, which a daily term of fixed
shape cannot remove. This is exactly the structure the conditional daily term was
designed for, and Section~\ref{sec:seasonal-fitted} found that the term did
not lower the held-out error; what remains is small, known, and left in the
residual on purpose. The 12-hour line survives on the on-structure and ERA5
sets at 0.002 to 0.003. In the long band the largest peak on every set is
semi-annual, 181 to 183 days at powers of 0.17 to 0.24, followed by entries
at 325, 247, 220 and 150 days on the on-structure set. The semi-annual peak
is a finding to be read with care. The yearly term's order was fixed at one
by the diagnostic of D6, on a regression residual that certified no
semi-annual cycle; the residual of the full model, after a trend with twelve
changepoints whose segments are four to six months long, carries one.
Whether it is a component of the wall's annual response that the order-one
term cannot represent, or the imprint of the changepoint spacing on what
the trend leaves behind, this study does not decide: by D6's rule the order
is never raised beyond what the diagnostic scan supports, and the question
is carried to Section~\ref{sec:limitations}.

\begin{table}[H]
\centering
\caption{Periods certified by the Lomb--Scargle scan on the attribution
fit's residual, per set, each band ranked against its own continuum, as in
Table~\ref{tab:certified-periods}. Read from
\texttt{GM\_08b\_residual\_periods.csv}.}
\label{tab:residual-periods}
\begin{tabular}{llrrr}
\toprule
Set & Band & Rank & Period [days] & Power \\
\midrule
\input{../outputs/GM_08b_body.tex}\\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[H]
\centering
\caption{Residual diagnostics of the attribution fit, per set: the Ljung--Box
p-value at lags of one slot, one day and three days, and the residual's
standard deviation and median absolute deviation. Read from
\texttt{GM\_08\_residual\_diagnostics.csv}.}
\label{tab:residual}
\begin{tabular}{lrrrr}
\toprule
Set & Lag [slots] & Ljung--Box $p$ & Std [mdeg] & MAD [mdeg] \\
\midrule
\input{../outputs/GM_08_body.tex}\\
\bottomrule
\end{tabular}
\end{table}

% ===========================================================================
\section{Impulse response}
\label{sec:impulse}

Model~A takes the coupling's timing from Study~03 and imposes it: air
temperature enters instantaneously, radiation delayed by one hour, neither with
a time constant. Model~B is the same on-structure fit with that choice removed.
Air temperature and radiation enter as lagged regressors over thirty-six slots
of the native twenty-minute grid, twelve hours of history per driver, while
relative humidity stays contemporaneous and the trend, the yearly and daily
terms, the changepoints and the learning rate are those of the attribution fit.
The weight the fit learns at each lag is the driver's impulse response in
millidegrees per unit of the driver, and three numbers are read from it: the
gain, which is the sum of the weights; the delay, which is the weight-weighted
mean lag, the discrete counterpart of the transport delay Study~03's operator
applies as a whole-sample shift; and the time constant $\tau$ of the one-pole
step response fitted to the response's cumulative sum, with the coefficient of
determination of that fit. Table~\ref{tab:impulse} sets the three beside
Study~03's cell for each driver, and Figure~\ref{fig:impulse} draws the
weights with Study~03's operator overlaid as a gain-matched response.

\begin{table}[H]
\centering
\caption{The impulse response Model~B learned on the on-structure set,
reduced to a gain, a delay and a time constant per driver, beside the delay and
time constant Study~03 measured and Model~A imposes. The gain is the summed
weight in millidegrees per degree Celsius for air temperature and per
W\,m$^{-2}$ for radiation; the delay is the weight-weighted mean lag; $\tau$
and $R^2$ are the one-pole step response fitted to the cumulative weight and
how much of the cumulative weight's variance it explains, negative when it
explains less than the mean does. Read from
\texttt{GM\_10\_impulse\_response.csv}.}
\label{tab:impulse}
{\small\setlength{\tabcolsep}{4.5pt}
\begin{tabular}{lrrrrrr}
\toprule
Driver & Gain & Delay [h] & $\tau$ [h] & $R^2$ one-pole & Study~03 delay [h] & Study~03 $\tau$ [h] \\
\midrule
\input{../outputs/GM_10_body.tex}\\
\bottomrule
\end{tabular}}
\end{table}

For air temperature the learned response agrees with the imposed one, and the
table's delay column has to be read with the weights in view to see it. The
gain is $-2.668$\,mdeg per degree, within 4.4\,\% of Study~03's $-2.79$ and
within 1.1\,\% of the $-2.639$ Model~A learned with the driver entering
instantaneously (Table~\ref{tab:gains}). Of that gain, $-1.856$\,mdeg per
degree, 69.6\,\%, sits in the first lag, the twenty minutes before the
observation, and $-2.283$, 85.6\,\%, in the first hour; the remaining
thirty-three weights sum to $-0.384$, change sign repeatedly and none exceeds
0.276 in magnitude. The 1.44\,h in the delay column is the first moment of
that whole sequence, and 1.08\,h of it comes from the small alternating
weights between one and twelve hours, against 0.36\,h from the first hour
that holds six sevenths of the gain. The one-pole fit says the same thing in
its own way: the cumulative weight reaches $-1.856$ at the first slot and then
stays between $-1.84$ and $-2.77$ over the remaining lags, a step and not an
exponential rise; the best one-pole time constant is 0.5\,h, and even that
shape explains the cumulative weight worse than its own mean does, hence the
negative $R^2$ of $-0.466$. At twenty minutes the wall's air-temperature response has no
resolvable delay and no time constant, which is Study~03's cell of zero and
zero, and the operator Model~A imposes is the one Model~B finds.

For radiation the learned response has no shape at all. The gain is
$+0.023$\,mdeg per W\,m$^{-2}$, positive as Model~A's $+0.006$ on this set
was and opposite in sign to the $-0.035$ Study~03 measured on the diurnal
band; the correction of the weights into physical units cannot have produced
the sign, since it multiplies every weight by a ratio of two positive scales.
No lag stands out. The largest weight, $0.0064$, is at the second lag, forty
minutes before the observation; the next two, $0.0049$ and $0.0038$, are at
the last lag and at six hours forty; twenty-four of the thirty-six weights
are positive and twelve negative; the weight at one hour, the lag at which
Study~03 places the response, is $-0.0026$. The first hour holds 9.4\,\% of
the gain and the weights beyond six hours as much as the weights within them,
$0.0111$ against $0.0118$, so the first moment falls at 6.5\,h, close to the
6.2\,h at which thirty-six equal weights over the same lags would put it: the
moment of a sequence with no preferred lag. The one-pole fit reports $\tau = 8.0$\,h with $R^2 = 0.929$, and this
is the one number in the table that has to be disbelieved: the cumulative sum
of thirty-six small weights of the same average sign climbs almost linearly
from $0.002$ at one hour to $0.023$ at twelve, and a line across a
twelve-hour window is what a one-pole response with a time constant of the
window's order draws. The fit certifies the drift of the weights, not a
response of the wall. Model~B therefore neither confirms nor refutes the
one-hour radiation delay Study~03 measured. It repeats the finding of
Section~\ref{sec:regressors}: once air temperature has taken its share of
the level, the marginal weight of the radiation that heats with it is
indistinguishable from noise at every lag the grid resolves, and the sign of
its sum is a finding stated, not a coupling recovered.

Checkpoint~4 is therefore passed for air temperature by agreement and left
open for radiation by the absence of any response to compare. Model~A is not
re-tuned on either count. The one-hour radiation delay stays imposed, as D9
provides, since the evidence for it is Study~03's diurnal-band scan and the
evidence against it is none; and the instantaneous air-temperature coupling
is now validated at the resolution Study~03's hourly scan could not reach.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{GM_F07_impulse_response.png}
\caption{The impulse response Model~B learned on the on-structure set, one
panel per driver: the weight at each of the thirty-six lags of the
twenty-minute grid, in millidegrees per unit of the driver, with Study~03's
operator overlaid as a response of the same gain. The air-temperature response
is one slot deep; the radiation response has no lag that stands out of the
sequence.}
\label{fig:impulse}
\end{figure}

% ===========================================================================
\section{Uncertainty and validation}
\label{sec:uncertainty}

Three things are validated here: that the attribution fit converged and where
its held-out error settles; that its components hold across the library's
chronological folds; and that the rolling expectation of D5, with the
rolling conformal interval of D8, is accurate and covers at the rate it
claims, as a function of how stale the last refit is.

\subsection{The fit and the folds}

Figure~\ref{fig:fit-metrics} draws the library's own training curve for the
on-structure attribution fit, the mean absolute error on the training rows
and on the held-out last fifth at every epoch; the values behind it are in
\texttt{GM\_05e\_fit\_metrics.csv}. The training error falls through the
thirty epochs and settles at 6.1\,mdeg; the held-out error reaches its
minimum of 11.5\,mdeg after six epochs and then rises to the 17.7\,mdeg at
the thirtieth that Table~\ref{tab:trend-reg} reports for the same fit. That
divergence is not a defect of the optimiser: the
held-out rows are the last fifth of a level record, from March 2025 onward, and
a model fitted once on the years before them extrapolates its last trend
segment across them. The same thing, measured across five folds, is what
Table~\ref{tab:stability} showed in Section~\ref{sec:regressors}: the gain
and the yearly amplitude are stable, the held-out error of a fold is not, and
it ranges from 7.8 to 100.7\,mdeg on the on-structure set according to where
the fold's window ends. A single fit cannot be the expectation; the
expectation has to be refitted as the record grows, which is D5.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{GM_F03_fit_metrics.png}
\caption{The training curve of the on-structure attribution fit: mean absolute
error on the training rows (solid) and on the held-out last fifth (dashed) by
epoch, in millidegrees.}
\label{fig:fit-metrics}
\end{figure}

\subsection{The rolling expectation, by days since refit}

The expectation was refitted every thirty days from 20~November 2020, the
first origin with two annual cycles of history behind it, to the end of the
record, each refit on the trailing three years with its changepoints placed
on the covered time of that window, and each refit's interval calibrated by
split conformal prediction on the previous six months of its own
out-of-sample residuals (D5, D8). Every row of the record from that origin
onward is therefore predicted by a model that has not seen it, from an origin
at most thirty days old. Table~\ref{tab:nowcast} scores the result per
regressor set and per week of staleness: 91,187 rows on the on-structure set,
88,414 on the station set and 100,583 on ERA5.

Accuracy decays with staleness, and it decays the same way on every set. On
the on-structure set the mean absolute error is 5.31\,mdeg in the first week
after a refit, 7.54 in the second, 9.05 in the third and 10.89 in the fourth,
a growth of about 1.9\,mdeg per week; the station set runs from 5.90 to 12.09
and ERA5 from 6.77 to 13.08. The bias stays within $\pm 1.2$\,mdeg in every
bin. Pooled over the month, the mean absolute error is 8.40\,mdeg on the
on-structure set, 9.38 on the station set and 10.19 on ERA5: the nearer the
driver, the better the expectation, by about one millidegree per step
outward. The growth within the month is larger than the trend line's own
extrapolation error, which D5 estimated at a fraction of a millidegree over
thirty days; what accumulates between refits is the residual's slow
wandering, the memory Section~\ref{sec:regressors} found in the residual
and D7 kept out of the model, which each refit re-anchors and the next month
lets drift again.

Coverage decays with staleness too, and this is the interval's finding. The
nominal 90\,\% interval covers 96.2\,\% of the on-structure rows in the first
week, 90.2\,\% in the second, 87.3\,\% in the third and 81.9\,\% in the fourth;
the station and ERA5 sets run from 97.1 to 81.0\,\% and from 95.5 to 78.1\,\%.
Its width does not change with staleness: about 35\,mdeg on the on-structure
set, 39 on the station set and 46 on ERA5 in every bin, because the
calibration pools the previous six months of residuals across every
staleness and so fixes one width for the whole month. The interval is
therefore right on average and wrong at both ends, too wide in the week after
a refit and too narrow in the week before the next. Pooled over the month it
covers 88.4\,\% of the on-structure rows, 88.0\,\% of the station set's and
86.5\,\% of ERA5's, against the nominal 90\,\% and against the 67.7\,\% that
Study~04 reached with an interval calibrated once, eleven months before the
record it was asked to cover. Two remedies follow from the table and are not
applied here: a calibration conditioned on staleness, which would let the
width grow through the month, or a shorter refit interval, which would cut
the month off before the fourth week; either is a design choice for the
deployment rather than for this study, and the interval score of the last
column, which charges both width and misses, is the number on which to
compare them.

\begin{table}[H]
\centering
\caption{The rolling expectation scored per regressor set and per week since
the last refit: rows, mean absolute error, root mean square error and bias in
millidegrees, the coverage and mean width of the nominal 90\,\% conformal
interval, and the interval score. Read from
\texttt{GM\_09\_nowcast\_metrics.csv}.}
\label{tab:nowcast}
{\small\setlength{\tabcolsep}{4.5pt}
\begin{tabular}{llrrrrrrr}
\toprule
Set & Staleness & Rows & MAE & RMSE & Bias & Coverage & Width & Interval score \\
\midrule
\input{../outputs/GM_09_body.tex}\\
\bottomrule
\end{tabular}}
\end{table}

Figure~\ref{fig:observed-expected} shows one month of the on-structure
expectation against the record, with its interval: December 2025, the first
full month after the autumn 2025 outage in which every driver of the set is
present, since the station's radiation is missing for most of November. The daily
cycle of the observed inclination is followed in phase and in size, the
expected level sits a few millidegrees above the observed one for most of the
month and the interval covers it throughout: a departure of that shape and
size, sustained across a refit month, is exactly what the daily and slow
charts of Section~\ref{sec:monitor} are built to judge, and the interval,
which asks only whether a reading is plausible given the last refit, is not
the instrument for it.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{GM_F08_observed_expected.png}
\caption{Observed inclination against the rolling expectation with its
90\,\% conformal interval, on-structure set, December 2025, on the
twenty-minute grid.}
\label{fig:observed-expected}
\end{figure}

% ===========================================================================
\section{What the wall temperature and the pyranometer buy}
\label{sec:ladder}

The probe and the pyranometer were installed on 21~February 2025 to find out
whether they carry information the other drivers do not, and D4 tests them
as a ladder on the current era alone. The window is the one on which every
channel exists at once: the probe's two valid blocks and the pyranometer's
certified days, 13,914 slots of twenty minutes, about 193 days. Every rung is
fitted and scored on those rows, so that a rung differs from the one below in
its channels alone; the first rung, which reads the station's radiation
instead of the wall's, holds 13,290 of them, the difference being the
station's own dropouts inside the window. Each rung fits Model~A, with the
plain daily term and the parameters of Section~\ref{sec:trend}, on the first
four fifths of its rows and scores the last fifth. Two cautions govern the
reading. The comparison between rungs is the paired block-bootstrap skill on
their shared held-out timestamps, not the mean absolute error column, which is
each rung's own held-out fifth and, for the first rung, a slightly different
stretch. And the ladder is the one place in this study where a model is fitted
on less than two annual cycles, by D4's design: its gains compare channels
against each other on one window and are not estimates of the wall's
parameters; those are Section~\ref{sec:regressors}'s.

Table~\ref{tab:ladder} and Figure~\ref{fig:ladder} give the result, rung by
rung. Replacing the station's radiation with the wall's own pyranometer, on
the same rows, raises the held-out error by 2.3\,\% (bootstrap bounds 1.6 to
3.2\,\%). The pyranometer's learned gain is $-0.002$\,mdeg per W\,m$^{-2}$,
against the $-0.026$ Study~03 measured for it on the diurnal band and the
$-0.008$ the station's column learns on the rung below; Section~\ref{sec:record}
had already found that its clock fails Study~02's agreement test. Adding the
wall temperature at zero delay with a four-hour time constant, the deployable
form, changes nothing over the rung below (skill 0.000, bounds $-0.3$ to
$+0.4$\,\%) and leaves the rung 2.2\,\% below the first (bounds 1.2 to
3.3\,\%). Its learned gain is $-0.30$\,mdeg per degree, a sixth of the
$-1.75$ Study~03 measured on the diurnal band: in a fit on levels that
already carries the air temperature and a trend, the probe's slow component
is shared with both, and what remains for it is small. The residual's
lag-one autocorrelation falls down the ladder, from 0.827 on the first rung
to 0.801, 0.798 and 0.789, so each channel absorbs a little of the residual's
memory without lowering its size. The diagnostic fourth rung, the probe at
its measured lead of two hours, which reads the future and can never serve
an expectation, improves on the third by 1.3\,\% (bounds 0.7 to 1.9\,\%) with
a gain of $-0.68$\,mdeg per degree, and still does not reach the first rung
(skill against it $-1.1$\,\%, bounds $-2.9$ to $+0.5$\,\%). The coverage
column is the raw quantile interval of a model fitted once. The held-out
fifth of the matched rows, 2,783 slots or about 39 days on rungs 2 to 4 and
2,658 slots on the first, is the tail of the probe's second valid block, 18~June
to 10~August 2026, so the fit that must hold there was made on the first block,
of 2025, and the opening two weeks of the second, across the eleven months in
which the probe recorded nothing. At 10 to 27\,\% against a nominal 90\,\%
the raw interval says what Study~04 found and D8 corrects, and it is not the
study's interval, which Section~\ref{sec:uncertainty} calibrates.

The verdict per channel, in D4's terms. The pyranometer: a deployment gains
nothing from it for the expectation, on this window; the station's radiation,
an hour away, serves the model at least as well, and the wall's own instrument
earns its place only if something other than the expectation wants it. The
wall-temperature probe: in its deployable form it buys nothing measurable over
the drivers the model already has, and its information about the wall arrives
two hours before the deformation, where a monitor cannot use it; a deployment
should not count on it for the expectation, and Study~03's finding that it is
an internal state variable rather than a driver stands.

\begin{table}[H]
\centering
\caption{The current-era ladder, on the matched window from 21~February 2025:
rows fitted and scored, held-out mean absolute error of the last fifth, the
paired block-bootstrap skill over the rung below and over the first rung with
their 5 and 95\,\% bounds, the raw quantile interval's coverage of the
held-out fifth, the learned gain of the rung's last channel (radiation in
mdeg per W\,m$^{-2}$, wall temperature in mdeg per degree) and the residual's
lag-one autocorrelation. Read from \texttt{GM\_16\_current\_era\_ladder.csv}.}
\label{tab:ladder}
{\scriptsize\setlength{\tabcolsep}{3.5pt}
\begin{tabular}{lrrrrrrrrrrr}
\toprule
& & & \multicolumn{3}{c}{Skill over rung below} & \multicolumn{3}{c}{Skill over rung 1} & & & \\
\cmidrule(lr){4-6} \cmidrule(lr){7-9}
Rung & Rows & MAE & Skill & 5\,\% & 95\,\% & Skill & 5\,\% & 95\,\% & Cov. & Gain & $r_1$ \\
\midrule
\input{../outputs/GM_16_body.tex}\\
\bottomrule
\end{tabular}}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{GM_F14_ladder.png}
\caption{The current-era ladder. Top: held-out mean absolute error per rung.
Bottom: the paired block-bootstrap skill of each rung over the one below,
with its 5 and 95\,\% bounds; zero means the added or replaced channel
changed nothing.}
\label{fig:ladder}
\end{figure}

% ===========================================================================
\section{The monitor}
\label{sec:monitor}

\subsection{The reference window and the four charted series}

Every control limit in this section is a multiple of a centre and a scale
estimated on a reference window, and D10 planned that window as the three
years 2019 to 2021. The residual the charts run on is the rolling
expectation's of Section~\ref{sec:uncertainty}, and that residual exists only
from the first refit origin, 20~November 2020, the first instant with two
annual cycles of history behind it. A window opening in 2019 would calibrate
the charts on rows that do not exist, so the window is 21~November 2020, the
first full day of the rolling residual, to 31~December 2021: 406 days, about
thirteen months, the last full year before the 2022 outages and before the
instrument change, with no outage longer than eleven days inside it. The
window is not widened, since a window containing a departure would calibrate
the detector against the very thing it is meant to find. Two consequences
follow and are carried through the section. The slow chart's budget of a year
per false alarm admits at most one false episode inside a window that short,
so its tuning rests on a single episode, and the injection dates for the
detectability sweep must all fall inside the same thirteen months.

The on-structure set's rolling residual is reduced to the four series of D10's
table. The fast chart runs on the residual's prewhitened innovations at twenty
minutes, $e(t) = r(t) - \phi\, r(t - 1)$, with $\phi = 0.996$ estimated as the
lag-one autocorrelation on the reference window, so that the limit is set
against the residual's own innovation scale rather than against a series that
keeps 99.6\,\% of its value from one slot to the next; the prewhitened
innovation's spread is well below the residual's, which is what
brings the fast chart's limit from the 14.75 standard deviations Study~04
needed on the raw residual to the 6.25 of Table~\ref{tab:run-lengths}. The
daily chart runs on the amplitude and the phase of a 24-hour harmonic fitted
to each day's residual, a day counting only when it holds at least 60 of its
72 slots. The slow chart runs on the daily mean residual. Each series carries
its own smoothing constant, coincidence window and false-alarm budget, fixed
before any sweep: 90 watched days per false alarm on the fast and daily charts,
365 on the slow one. The design placed the sequence of refit trend slopes
beside the slow chart; that companion was not built, and the trend rates of
Section~\ref{sec:trend} describe the record's slow movement without
monitoring it.

\subsection{The limits, and the run lengths achieved}

Each chart's control limit is the smallest candidate, on a grid of a quarter
of a standard deviation from 1.0 to 15.0, whose average run length between joint alarms
on the reference stretch meets that chart's budget; the CUSUM slack and
decision interval, 0.5 and 5 standard deviations, are Study~04's and are
shared by every chart. Table~\ref{tab:run-lengths} gives the result. The fast
chart meets its budget at 6.25 standard deviations with a run length of
135~days on three reference episodes; the daily-amplitude chart at 3.25 with
102~days on four; the slow chart at 13.5 with 406~days on one, the whole
window, so its run length is a bound set by a single episode rather than a
measurement. The daily-phase chart sits at 1.00, the floor of the sweep, with
203~days on two episodes, and it sat on the floor of the first run's sweep,
2.00, with the same run length on the same two episodes. That is not a defect
of the grid: the joint alarm requires the EWMA and the CUSUM to agree, and at
these limits the CUSUM's decision interval, not the EWMA's limit, gates the
phase chart, so lowering the floor further would change nothing. The phase
chart is therefore as sensitive as D10's shared CUSUM allows, and its run
length exceeds its budget by a factor of two.

\begin{table}[H]
\centering
\caption{The control limit each chart met its budget at, the run length it
achieves on the reference window and the number of reference episodes that
run length rests on. Read from \texttt{GM\_12\_run\_lengths.csv}.}
\label{tab:run-lengths}
\begin{tabular}{lrrrr}
\toprule
Chart & $L$ & Budget (d) & Achieved (d) & Episodes \\
\midrule
\input{../outputs/GM_12_body.tex}\\
\bottomrule
\end{tabular}
\end{table}

\subsection{What the charts reported}

Figures~\ref{fig:fast-chart} to~\ref{fig:slow-chart} run the three charts over
the monitored record, from January 2022 to the archive end, and
Table~\ref{tab:episodes} lists every episode the joint alarm raised, 99 in
all: 76 on the fast chart, 17 on the daily-amplitude chart, 3 on the
daily-phase chart and 3 on the slow one.

Every fast alarm is cross-checked against the on-structure air temperature,
relative humidity and supply voltage in the same slot, the test Study~01 ran by
hand on the summer of 2026. A channel counts as in excursion when its departure
from a centred two-hour rolling median exceeds five times that departure's
robust scale on the reference window, and an episode's attribution is the
majority label of its slots. The window is two hours rather than the day the
first run used: over a day the departure from the median on air temperature is
the diurnal cycle itself, whose swing no realistic excursion reaches, and every
episode came back unattributed; a coincidence test built to catch a
twenty-minute swing must measure that swing against its own local background.
With the two-hour window 69 of the 76 fast episodes are attributed to the
instrument, two to the environment and five to neither. The 52 episodes from
18~June to 10~August 2026, one or two a day, are the summer-2026 stretch
Study~01 flagged; they begin the day after the archive resumed on 17~June
2026, they coincide to the day with the wall-temperature probe's second valid
block of Section~\ref{sec:ladder}, and every one of them is attributed to the
instrument through the supply-voltage channel. That is the demonstration D10
asked of the attribution step and not a claim about the monitor's
sensitivity: the fast chart alarms on a stretch the record's own supply
channel already marks, and says so. The sixteen episodes between 11~March and
26~August 2025, in the months after the instrument change of 21~February 2025,
carry the same label, and so does one slot on the morning of 30~October 2024.
The five unattributed episodes, the ones the monitor leaves to the wall to
answer for, fall on four dates: two on 8~July 2023, the second of them twelve
hours long and the largest standardised excursion of the record at a peak of
95.7, and one each on 3~April 2024, 2~July 2024 and 30~October 2024. Three of
the four dates fall 17 to 21 days after a resumption: 21~June 2023 after the
437-day gap, 14~June 2024 and 9~October 2024 after the two outages of that
year. The two environment labels are one and two slots at the onset of the
July 2023 and July 2024 episodes, where the air temperature or humidity also
jumped.

The daily charts see the same stretches at their own scale. The amplitude
chart's longest episodes are the 33 days from 8~July 2023, at a mean of 3.8
standard deviations, the 48 days from 23~August to 9~October 2024, which
span the outage of 28~August to 8~October so that most of their days are the
gap itself, held in alarm by the average's last value before it, and the 55 days from 19~June to 12~August 2026 at a mean of 4.4,
the summer-2026 stretch again, whose daily amplitude peaks at 8.0 standard
deviations in the top panel of Figure~\ref{fig:daily-chart}. The phase chart
raises three episodes, two in October 2023 and one of 44 days in the same
summer of 2026. The slow chart raises three: 26~June to 12~July 2023 at a mean
of $+4.8$ standard deviations and 23 to 31~October 2024 at $+3.9$, beginning
five and fourteen days after a resumption, and 10 to 12~August 2026 at $-4.3$,
at the archive's end. What the wall did across each of those outages,
and whether the level the model expected at resumption is the one observed,
is the question Section~\ref{sec:outages} puts to the proxies that kept
recording; the charts of this section report that the weeks after each
resumption are where the record departs from its expectation on every time
scale at once.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{GM_F09_fast_chart.png}
\caption{The fast chart over the monitored record: the exponentially weighted
moving average of the prewhitened innovations, standardised on the reference
window, with the tuned limit; alarming episodes shaded. The average keeps its
last value where the residual is missing, so the line runs flat across a gap
and an alarm raised before a gap persists through it.}
\label{fig:fast-chart}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{GM_F10_daily_chart.png}
\caption{The daily chart: the amplitude (top) and phase (bottom) of the
24-hour harmonic fitted to each day's residual, standardised on the reference
window, with their tuned limits and their episodes shaded.}
\label{fig:daily-chart}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{GM_F11_slow_chart.png}
\caption{The slow chart: the upper CUSUM of the daily mean residual against
its decision interval, episodes shaded.}
\label{fig:slow-chart}
\end{figure}

{\scriptsize
\begin{longtable}{llrrrl}
\caption{Every episode the joint alarm raised over the monitored record, by
chart: start and end dates, duration, the mean standardised departure over
the episode and, for the fast chart, its attribution. Read from
\texttt{GM\_11\_alarm\_episodes.csv}.}
\label{tab:episodes}\\
\toprule
Chart & Start & End & Duration (h) & Mean $z$ & Attribution \\
\midrule
\endfirsthead
\toprule
Chart & Start & End & Duration (h) & Mean $z$ & Attribution \\
\midrule
\endhead
\bottomrule
\endfoot
\input{../outputs/GM_11_body.tex}\\
\end{longtable}}

\subsection{What each chart can and cannot see}

One large event cannot establish a detector's sensitivity, and the summer-2026
stretch is not used to try. Departures of known size, duration and shape are
injected into the reference window's own residual, the only stretch known to
be in control, on four dates across the seasons, 15~December 2020 and 15~March,
15~June and 15~September 2021, and an alarm counts only where the
uncontaminated record is silent within a twenty-four-hour response window. The
detected fraction is therefore the share of the four dates on which the
departure was found. The four mechanisms are Study~04's three, with the two
corrections of D11, and a step. Amplitude growth is injected at 0.5 to 16~mdeg.
The phase change is injected as a timing shift of a quarter, half, one and two
hours, converted to a residual amplitude by the wall's own fitted daily
response on each injection date, the air-temperature component plus the daily
seasonal terms: 0.69, 1.39, 2.77 and 5.49~mdeg, against the 5.6 the design
estimated for the two-hour shift from the numbers it had and the 1.07
Study~04 injected for it on the leftover daily term alone. The drift is
injected as a rate of 1 to 200~mdeg per year and, unlike the other mechanisms,
scored over horizons of 30, 60 and 90 days rather than of 6 to 336 hours,
because a rate accumulated over fourteen days never amounts to anything a
chart could see. The step is injected at the same magnitudes as the amplitude.
Each mechanism is scored on the chart built for it and on the other three as
well. Table~\ref{tab:detectability} gives the sweep on each mechanism's own
chart, Figures~\ref{fig:detect-amplitude} to~\ref{fig:detect-step} draw it,
and Table~\ref{tab:thresholds} reduces the whole sweep, own charts and the
others, to the two numbers a reader wants: at the longest horizon swept, the
smallest departure a chart catches on any date and the smallest it catches on
every date.

Amplitude growth is the mechanism the monitor sees. On the daily-amplitude
chart a growth of 8~mdeg is found on every date at every duration, from six
hours upward, and 4~mdeg on every date at a week's duration and on three dates
in four at two weeks; 2~mdeg is found on one date in four at most durations,
on two at a week, and 1~mdeg on one date at the two shortest durations only.
Study~04, on one injection date and the raw residual, found amplitude growth
at 16~mdeg alone. The delay grows with the duration at a given magnitude, from
six hours at 8~mdeg and a six-hour injection to 156~hours at the two-week one,
because a longer injection is scored over a longer window and the alarm that
counts sits later in it.

The phase change is the mechanism the monitor does not see on its own chart.
No shift is found on all four dates; a one-hour shift, 2.77~mdeg, is found on
one date in four at the two-week duration, and a two-hour shift, 5.49~mdeg, on
one date in four from a week upward. The same two-hour shift is found on every
date by the amplitude chart, with a delay of 186~hours, since a sinusoid added
to the residual at a shifted phase raises the fitted daily amplitude as well
as moving its phase. Study~04's verdict that a timing shift of up to two
hours is undetectable stands for the phase chart; the amplitude chart
qualifies it at the top of the swept range.

The drift is found on the slow chart only at rates far above the ones D10 had
in mind. A rate of 200~mdeg per year is found on every date over 90 days, with
a delay of 1,128~hours, 47 days, and on three dates in four over 60 days; 100
per year is found on two dates over 90 days. From 10~mdeg per year upward one
date in four alarms at every rate and every horizon, which reads as one
injection date already sitting near the limit rather than as sensitivity to the
rate, since the fraction does not rise with either. Study~04's grid of 1 to
20~mdeg per year is not detected on every date at any horizon, as it was not
in Study~04, and the slow chart's limit, set by a single reference episode at
13.5 standard deviations, is where that blindness comes from.

The step is the mechanism the fast chart was built for, and it is found only
at 16~mdeg, on three dates in four, at every duration and with no delay, and
not at all below. The reason is the prewhitening itself. With $\phi = 0.996$ a
level step of size $s$ becomes a single innovation of size $s$ followed by
innovations of $(1 - \phi)\,s$, four thousandths of the step, so a step is one
slot's spike to the fast chart; the EWMA at a smoothing constant of 0.05
retains a twentieth of that spike and the CUSUM accumulates it for one slot.
The chart finds spikes and instrument faults, which is what the record's
episodes are, and leaves a sustained level shift to the slow chart, which
does see the 16~mdeg step on one date. Across charts, drift reaches the fast
chart from 20~mdeg per year and the daily-phase chart from 100, on one date
each, and the daily-amplitude chart from 100 on three dates in four; amplitude
growth reaches the fast chart only at 16~mdeg, where it is found on every date.

The blind spots, stated at the budgets of Table~\ref{tab:run-lengths}: a
level step below 16~mdeg, a timing shift of two hours or less on the phase
chart, and a drift below 100~mdeg per year within 90 days. What the monitor
does catch on every date is an amplitude growth of 8~mdeg within hours and of
4~mdeg within a week, a two-hour timing shift within eight days on the
amplitude chart, and a drift of 200~mdeg per year within seven weeks.

{\scriptsize
\begin{longtable}{llrrrr}
\caption{Detectability by mechanism on its own chart, at the stated budgets:
injected magnitude (mdeg, or mdeg per year for the drift), duration or
horizon, the fraction of the four injection dates on which the departure was
found, and the mean detection delay where it was. Read from
\texttt{GM\_13\_detectability.csv}, primary rows.}
\label{tab:detectability}\\
\toprule
Mechanism & Chart & Magnitude & Duration (h) & Detected & Delay (h) \\
\midrule
\endfirsthead
\toprule
Mechanism & Chart & Magnitude & Duration (h) & Detected & Delay (h) \\
\midrule
\endhead
\bottomrule
\endfoot
\input{../outputs/GM_13_body.tex}\\
\end{longtable}}

\begin{table}[H]
\centering
\caption{The detection threshold of every mechanism on every chart, read at
the longest horizon swept: the smallest magnitude found on any injection date,
the smallest found on every date, and the delay at that magnitude; a dash
where no magnitude in the sweep was found. The primary column marks the chart
built for the mechanism. Read from
\texttt{GM\_13b\_detection\_thresholds.csv}.}
\label{tab:thresholds}
{\small\setlength{\tabcolsep}{4pt}
\begin{tabular}{llrrrrl}
\toprule
Mechanism & Chart & Horizon (h) & Smallest any & Smallest all & Delay (h) & Primary \\
\midrule
\input{../outputs/GM_13b_body.tex}\\
\bottomrule
\end{tabular}}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{GM_F13_detectability_amplitude.png}
\caption{Amplitude growth on the daily-amplitude chart: the fraction of
injection dates detected (left) and the mean detection delay (right) by
magnitude and duration.}
\label{fig:detect-amplitude}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{GM_F13_detectability_phase.png}
\caption{Phase change on the daily-phase chart, the timing shift expressed as
the residual amplitude the wall's fitted daily response gives it.}
\label{fig:detect-phase}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{GM_F13_detectability_drift.png}
\caption{Drift on the slow chart, by rate in mdeg per year and horizon in
hours.}
\label{fig:detect-drift}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{GM_F13_detectability_step.png}
\caption{Step on the fast chart. Nothing below 16~mdeg is found at any
duration: prewhitening turns a level step into a single slot's innovation.}
\label{fig:detect-step}
\end{figure}

% ===========================================================================
\section{Outages as hypotheses}
\label{sec:outages}

For each of the seven outages of seven days or more that D12 lists, Model~A is
fitted once on every row before the outage's first missing day, on the
station set and on the ERA5 set, the two whose drivers kept recording, and
predicts the inclination over the first days back. The expected level is the
mean of that prediction over the seven days that follow the settle day after
resumption, the observed level is the mean of the accepted readings over the
same days, the shift is their difference, and the verdict says whether the
observed level lies inside the mean quantile band of the fit; an outage with
fewer than 24 observed slots in that window, or with less than two annual
cycles of history before it, is reported as having no data.
Table~\ref{tab:bridges} gives the result and Figure~\ref{fig:bridges} draws
the five bridges the station set could build. Two things about the method
are stated before any verdict is read.

The first is the interval. D12 named the conformal interval of
Section~\ref{sec:uncertainty}; that interval is calibrated on the residuals
of the rolling expectation over the days before each origin, and inside a
gap there are no residuals to calibrate on, so the band a bridge can carry is
the quantile band of the single fit made before the outage. That band is the
raw interval of Sections~\ref{sec:uncertainty} and~\ref{sec:ladder}, which
under-covers: between 4 and 17\,mdeg wide on every bridge of
Table~\ref{tab:bridges} but the 2026 one, against the 39\,mdeg (station) and 46\,mdeg (ERA5)
the calibrated interval of Table~\ref{tab:nowcast} needs to hold 90\,\% of
the readings in the first week after a refit. A verdict of outside against
the band therefore says less than it seems, and the shift is read against the
calibrated half-width as well, about 19\,mdeg for the station set and
23\,mdeg for ERA5. The second is the drawing. The modelling frames keep no
row whose target is missing (D13), so the expectation is predicted, and
drawn, from resumption onward; what the model would have expected inside the
gap is neither drawn nor needed for the verdict, which compares levels after
the gap.

The outage of June 2020 has no data because it falls before the first instant
with two annual cycles of history behind it, 20~November 2020, the same rule
that fixed the rolling expectation's first origin. The outage of June to
September 2022 has no data of its own because, as Section~\ref{sec:record}
showed, the accepted target resumed only on 21~June 2023: the two outages of
2022 and the condemned weeks around them merge into the single gap of
437.6~days, so the bridge across it is the third row's, fitted on every row
before 23~September 2022, which in the accepted record means every row before
9~April 2022, and carrying its expectation across the whole 437 days rather
than the 271 the outage list names.

Across that gap the wall did not return to the level the model expected. The
station set expected 54.5\,mdeg and ERA5 45.7, the readings averaged 99.3, a
shift of $+44.8$ and $+53.6$\,mdeg on the two sets: outside the band, outside
the calibrated half-width by more than twice, and the largest shift in the
record. The top panel of Figure~\ref{fig:bridges} shows it as a daily cycle
of the right shape and period running some 45\,mdeg above its expectation
throughout the week. Section~\ref{sec:monitor}'s charts found the same
stretch from the other side: the fast, daily and slow charts all raised
episodes between five and seventeen days after this resumption, and the slow
chart's first episode of the record begins there. The two proxy sets, fed
the same readings, expect levels 8.7\,mdeg apart, which is the size of the
disagreement between them where the history before the gap is longest.

The outage of May to June 2024 is the one bridge on which nothing happened:
expected 33.3 and 33.5\,mdeg, observed 34.5, shifts of $+1.2$ and $+1.0$,
inside the band on both sets, and the second panel shows the readings riding
the expectation through every daily cycle of the week. The outage of
August to October 2024 is the opposite case again: expected 19.1 and 22.1,
observed 55.5, shifts of $+36.4$ and $+33.4$\,mdeg, outside the band and
outside the calibrated half-width on both sets, and Section~\ref{sec:monitor}'s
slow chart alarms from 23~October, fourteen days after this resumption, with
the unattributed fast episode of 30~October beside it. The outage of
September to October 2025 gives shifts of $+15.8$ and $+13.2$\,mdeg, outside
a band 7\,mdeg wide on both sets, but inside the calibrated half-width; on
the interval this study trusts, that outage hid no movement the model can
distinguish from its own error a week after a refit.

The outage of March to June 2026 is the one on which the sets disagree, and
the disagreement is not to be read as a finding about the wall. The station
set expected 92.3\,mdeg and finds the observed 69.4 outside, a shift of
$-22.9$; ERA5 expected 83.7 and finds 69.3 inside, a shift of $-14.4$; the
two bands are 46 and 36\,mdeg wide, two to eleven times any other bridge's,
because the fit that carries them was made on a history that ends in the
first year of the new instrument. And the resumption window, 18 to 25~June
2026, is the opening week of the stretch Section~\ref{sec:monitor} attributed
to the instrument through the supply-voltage channel: the bottom panel of
Figure~\ref{fig:bridges} shows the readings falling to between 20 and
40\,mdeg on six of the seven mornings where the expectation stays above
70. The observed mean is the instrument's, and the verdict on this outage is
left open until the record after August 2026 is read.

The verdict per outage, then, at the interval this study trusts: the 2022 to
2023 gap and the autumn 2024 outage each hid a rise of the level, of about 45
to 54 and 33 to 36\,mdeg by the two proxy sets, that the model built on the
history before them did not expect; the spring 2024 outage hid nothing; the
autumn 2025 outage's shift of 13 to 16\,mdeg is inside the calibrated
half-width; the 2026 outage cannot be judged from a resumption window the
instrument itself contaminates; and the two earliest outages leave nothing to
bridge. Whether the two rises are the wall's or the instrument's is not a
question a bridge can answer, since it sees only the level before and after;
what it establishes is that the level after each of those gaps is not the
level the drivers alone account for, and Section~\ref{sec:verdict} takes it
from there.

\begin{table}[H]
\centering
\caption{The outage bridges: for each proxy set and outage, the outage's
first and last missing day, the expected level over the seven days after the
settle day, the observed level there, their difference, the lower and upper
bounds of the fit's own quantile band, and the verdict against that band; no
data where fewer than 24 slots were observed or fewer than two annual cycles
preceded the outage. Millidegrees. Read from
\texttt{GM\_14\_outage\_bridges.csv}.}
\label{tab:bridges}
{\small\setlength{\tabcolsep}{4pt}
\begin{tabular}{lrllrrrrrl}
\toprule
Set & \# & Start & End & Expected & Observed & Shift & Lower & Upper & Verdict \\
\midrule
\input{../outputs/GM_14_body.tex}\\
\bottomrule
\end{tabular}}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[height=0.8\textheight,keepaspectratio]{GM_F12_outage_bridges.png}
\caption{The outage bridges on the station set, one panel per outage that
could be bridged: the accepted readings over the settle day and the seven
days after resumption, the expectation of the fit made before the outage,
and that fit's own quantile band; the settle day and the scored week are
shaded. The expectation
is drawn from resumption onward, since the modelling frame keeps no row
whose target is missing.}
\label{fig:bridges}
\end{figure}

% ===========================================================================
\section{Verdict}
\label{sec:verdict}

Five questions were asked in Section~\ref{sec:intro}. Each is answered here
in one paragraph, with the qualification that belongs to it, and every number
is one of the tables' own.

\textbf{What is the record made of, over eight years?} Net of its slow
movement, a thermometer. On the on-structure set the trend holds 60.1\,\% of
the fitted variance, air temperature 24.1\,\% with a peak-to-peak
contribution of 115.4\,mdeg, the yearly term 12.2\,\%, the residual 3.1\,\%,
and humidity, the daily term and radiation together under half a per cent
(Table~\ref{tab:shares}); the station and ERA5 sets give the same picture
with the driver's share falling as it is measured farther from the wall. The
air-temperature gain is $-2.639$\,mdeg per degree, within 5.4\,\% of the
$-2.79$ Study~03 measured on the diurnal band by a different method, and
$-2.402$ and $-2.295$ on the other two sets (Table~\ref{tab:gains}). The
yearly term, of order one as the harmonic diagnostic prescribed, peaks at the
equinoxes, in quadrature with the air: the delayed part of the annual
response. The daily term, of order two, is small, 3.9\,mdeg peak-to-peak, and
its seasonal modulation did not earn a conditional term. The trend is not a
drift: its rate reverses sign twice, from $+104$\,mdeg per year in the spring
and summer of 2019 to $-91$ in 2021, $-89$ after the 2023 resumption and
$+28$ in 2024 (Table~\ref{tab:trend-rates}). The qualification is
radiation: the one driver the fit does not recover, with a gain of
$+0.006$\,mdeg per W\,m$^{-2}$ against Study~03's $-0.035$, because on
levels its marginal contribution beyond the air temperature that heats with
it is close to zero; and the residual, 7.97\,mdeg in standard deviation, is
not white and carries a semi-annual line this study does not explain.

\textbf{Does the wall answer its drivers with a delay or an inertia the
20-minute grid can resolve?} For air temperature, no: the learned impulse
response holds 69.6\,\% of its $-2.668$\,mdeg per degree in the twenty
minutes before the observation and 85.6\,\% in the first hour, a step and not
an exponential, with no resolvable delay and no time constant, which is the
operator Study~03 measured hourly and Model~A imposes
(Table~\ref{tab:impulse}). For radiation the question has no answer, because
the learned response has no shape: no lag stands out, the weights beyond six
hours weigh as much as those within, and the sum is $+0.023$, opposite in
sign to Study~03's coupling. Model~B validates the instantaneous
air-temperature coupling at a resolution the hourly scan could not reach and
leaves the one-hour radiation delay imposed on Study~03's evidence alone.

\textbf{Is this reading the expected one?} With a stated confidence that
depends on the age of the last refit. The rolling expectation, refitted
every thirty days on the trailing three years, has a mean absolute error of
5.31\,mdeg in the week after a refit and 10.89\,mdeg in the fourth week on
the on-structure set, and its conformal interval, calibrated on the six
months before each origin, covers 96.2\,\% of the readings in the first week
and 81.9\,\% in the fourth, 88.4\,\% pooled over the month against the
nominal 90 and against the 67.7\,\% Study~04 reached with an interval
calibrated once (Table~\ref{tab:nowcast}). The qualification is that the
interval's width, about 35\,mdeg, does not change with staleness, so it is
right on average and wrong at both ends, and that a departure sustained
across a refit month is not what the interval judges. On the current era,
neither the wall-temperature probe nor the on-structure pyranometer buys the
expectation anything: the pyranometer raises the held-out error by 2.3\,\%
against the station's radiation, and the probe in its deployable form
changes nothing (Table~\ref{tab:ladder}).

\textbf{What departure does each chart catch, at a stated false-alarm
budget?} At budgets of ninety days per false alarm on the fast and daily
charts and a year on the slow one, met at 6.25, 3.25, 1.00 and 13.5 standard
deviations (Table~\ref{tab:run-lengths}): an amplitude growth of 8\,mdeg on
every injection date within hours and of 4\,mdeg within a week, on the
daily-amplitude chart; a drift of 200\,mdeg per year within seven weeks on
the slow chart; a step of 16\,mdeg on three dates in four, at once, on the
fast chart (Table~\ref{tab:thresholds}). The blind spots are as plainly
stated: a step below 16\,mdeg, which prewhitening reduces to one slot's
innovation; a timing shift of up to two hours on the phase chart, which the
amplitude chart catches only at the top of the range; and a drift below
100\,mdeg per year within ninety days. Over the monitored record the charts
raised 99 episodes (Table~\ref{tab:episodes}), and the attribution step did
what D10 asked of it: 69 of the 76 fast-chart episodes, the whole
summer-2026 stretch among them, are the instrument's by the supply-voltage
channel, and the five left to the wall fall in the weeks after a resumption.
The qualification is the reference window, thirteen months rather than
three years, on which the slow chart's tuning rests on a single episode.

\textbf{What happened across each outage?} The level after two of the five
gaps that could be bridged is not the level the drivers account for. Across
the 437-day gap of 2022 to 2023 the readings resumed 44.8 and 53.6\,mdeg
above what the station and ERA5 sets expected, and across the autumn 2024
outage 36.4 and 33.4\,mdeg above; the spring 2024 outage hid nothing, the
autumn 2025 shift of 13 to 16\,mdeg is inside the calibrated half-width, and
the 2026 outage resumed into the instrument's own stretch and is left open
(Table~\ref{tab:bridges}). The qualification is twofold: the band a bridge
carries is the pre-outage fit's raw quantile band, which under-covers, so
every shift is read against the calibrated half-width as well; and a bridge
sees only levels, so whether a rise is the wall's or the instrument's is a
question for the record on either side of the gap, not for the bridge. The
monitor's charts, which alarm on every time scale in the weeks after those
same resumptions, say that the departure is real in the residual; what
caused it, this study does not decide.

% ===========================================================================
\section{Limitations}
\label{sec:limitations}

The compensation is taken as given, and it is not independent of the fitted
term. The record is the manufacturer's thermally compensated inclination,
and every gain learned here is a post-compensation wall response: a
coefficient applied inside the instrument to the same air temperature the
model reads cannot be separated from the wall's own response to it, so the
$-2.64$\,mdeg per degree of Table~\ref{tab:gains} is the residual coupling
after that coefficient, and Study~03's finding that the compensation is
imperfect stands behind every figure of this report.

The on-structure set's radiation is borrowed. The wall's own pyranometer
exists only from February 2025 and lost 161 of its days to Study~01's
verdicts, so the on-structure set reads the station's radiation, an hour
away, delayed by the hour Study~03 measured for it; the set's air and
humidity are the wall's, its radiation is not, and Section~\ref{sec:ladder}
found that replacing the borrowed column with the wall's own instrument on
the one window where both exist made the expectation worse.

The reference window is thirteen months, not three years, and it ends three
years before the instrument change. The rolling residual exists only from
November 2020, so the monitor is calibrated on 406 days rather than the
1,096 D10 planned; the slow chart's tuning rests on a single episode, the
daily-phase chart is gated by the CUSUM decision interval every chart
shares, and the instrument installed in February 2025 is charted against
limits set on its predecessor's residual. The charts' verdicts on the
current era, including the summer-2026 stretch, are read with that in mind.

The interval through a gap is the pre-outage fit's own band. No conformal
calibration exists inside a gap, and the raw band under-covers by the
measure of Sections~\ref{sec:uncertainty} and~\ref{sec:ladder}, so the
outage verdicts of Section~\ref{sec:outages} are stated twice, against the
band and against the calibrated half-width, and the reader is asked to
prefer the second.

Two things the design named were not built, and one it did not foresee was
found. The sequence of refit trend slopes beside the slow chart was not
built, and the trend rates of Section~\ref{sec:trend} describe the record's
slow movement without monitoring it. The expectation is not drawn through a
gap, because a modelling frame keeps no row whose target is missing; the
verdict does not need it, but the picture D12 described is not the one
Figure~\ref{fig:bridges} draws. And the full model's residual carries a
semi-annual line, at 181 to 183 days on every set, that the diagnostic of
Section~\ref{sec:harmonic-diagnostic} did not certify on the regression
residual; whether it is a component of the wall's annual response that an
order-one term cannot hold or the imprint of the changepoint spacing on
what the trend leaves behind is left open, since by D6 the order is never
raised beyond what the scan supports.

The fast chart does not see a level step. Prewhitening, which brought its
limit from Study~04's 14.75 standard deviations to 6.25, turns a step into a
single innovation, and a sustained shift below 16\,mdeg passes it unseen;
the slow chart is where such a shift belongs, and the detectability sweep
found it there only at the top of the range. A monitor that must catch a
step of a few millidegrees within hours needs a chart this study did not
build.

Radiation is unresolved. Neither the attribution fit nor the learned impulse
response recovers the coupling Study~03 measured on the diurnal band, and
the study reports the sign of its learned gain as a finding rather than a
coupling; what radiation adds to the level beyond the air that heats with it
is, on this evidence, close to nothing, and what it does to the daily swing
is Study~03's question, not this one's.

One sensor, one site, one archive. Everything above is measured on the
inclinometer at station~02 of the Gubbio walls over eight years with the
outages Section~\ref{sec:record} describes; the method transfers, and the
numbers do not, until another sensor has been through the same sections.

% ===========================================================================
\section{Run metadata}
\label{sec:metadata}
\pending{Phase 7}

\end{document}
